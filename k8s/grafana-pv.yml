apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-pv
  labels:
    type: grafana
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /mnt/grafana