kind: Service
apiVersion: v1
metadata:
  name: ecommerce-backend-product-service
  namespace: ecommerce
  labels:
    app: backend-product
spec:
  type: NodePort
  selector:
    app: backend-product
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001